{
  "name": "d-arrow-business-functions",
  "version": "1.0.0",
  "private": true,
  "description": "ZATCA Phase 2 e-invoicing library (signing, hash chain, clearance/reporting). Served in production by Vercel /api/zatca; optional Cloud Functions wrappers remain for Blaze projects.",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "onboard": "npm run build && node lib/scripts/onboard.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "node-forge": "^1.3.1",
    "xml-crypto": "^6.0.1",
    "xmldom": "^0.6.0",
    "axios": "^1.15.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-forge": "^1.3.11",
    "typescript": "^5.5.0"
  }
}
